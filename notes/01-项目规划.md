# 项目规划

## 技术栈
- 框架：Next.js 16 + React 19
- 语言：TypeScript
- 样式：Tailwind CSS + Shadcn UI
- 数据：Supabase
- 部署：Vercel 

## 🎯 目标
构建一个具有 **Vercel Aesthetic (极简工业风)** 的网站推荐平台。

### 📅 阶段一：基础设施搭建
- [x] 初始化 Next.js 16 项目
  - [x] 配置 Geist 字体 (Sans & Mono)(Next创建时自带了)
- [x] 初始化 Shadcn UI
- [x] 配置深色模式 (Dark Mode) 

### 🎨 阶段二：核心 UI 开发
- [x] **布局 (Layout)**: 顶部导航栏
- [x] **卡片组件 (Site Card)**: 设计极简的网站展示卡片，带 hover 边框发光效果。
- [x] **分类导航 (Categories)**: 顶部 Tab 切换。

### 🔌 阶段三：数据与功能
- [x] 集成 Supabase 数据库
- [ ] 实现网站数据的增删改查 (CRUD)

## 初始化

Next(Next16 + React19 + TS) 
```bash
pnpm create next-app@latest my-app --yes
cd my-app
pnpm dev
```
--yes 会跳过使用已保存的首选项或默认值的提示。默认设置启用 TypeScript、Tailwind、ESLint、App Router 和 Turbopack，导入别名 @/* 。

## 安装 Shadcn UI
```bash
pnpm dlx shadcn@latest init
```

### 安装 Shadcn UI 按钮
```bash
pnpm dlx shadcn@latest add button
```
引入按钮
```tsx
import { Button } from "@/components/ui/button";
```

## 图标 Lucide Icons
- Shadcn UI 安装时默认安装了 Lucide Icons。
  - [Lucide Icons 官网](https://lucide.dev/icons)
使用方法：
```tsx
import { Moon, Sun } from "lucide-react" 
```